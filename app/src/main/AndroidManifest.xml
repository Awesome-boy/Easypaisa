<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sdkdemo.kx.come.easypaisa">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="sdkdemo.kx.come.easypaylibrary.activity.PaymentActivity"/>
        <activity android:name=".PaymentActivity" />
        <activity android:name=".AuthorizationActivity" />
        <activity android:name=".GenerateQRActivity" />
        <activity android:name=".ParseQRActivity" />
        <activity android:name=".InquiryActivity" />
        <activity android:name=".VoidActivity" />
        <activity android:name=".RefundActivity" />
        <activity android:name=".PreAuthorizationActivity" />
    </application>

</manifest>